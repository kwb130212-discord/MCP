let csrf="",me={nickname:""},currentCategory="전체";const $=s=>document.querySelector(s);function toast(t){const e=$("#toast");e.textContent=t;e.style.display="block";clearTimeout(window._t);window._t=setTimeout(()=>e.style.display="none",2200)}async function api(url,opt={}){opt.headers={...(opt.headers||{}),...(csrf?{"x-csrf-token":csrf}:{})};const r=await fetch(url,opt);const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||"요청 실패");return d}function esc(s){return String(s).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]))}function date(s){return new Date(s).toLocaleString("ko-KR",{dateStyle:"short",timeStyle:"short"})}function theme(){const saved=localStorage.getItem("cat-theme");document.documentElement.dataset.theme=saved||"dark"}function toggleTheme(){const next=document.documentElement.dataset.theme==="light"?"dark":"light";document.documentElement.dataset.theme=next;localStorage.setItem("cat-theme",next)}async function session(){const r=await api("/api/session");me=r;csrf=document.cookie.split("; ").find(x=>x.startsWith("cat_hero_csrf="))?.split("=")[1]||"";$("#nickInput").value=me.nickname}async function load(){const d=await api("/api/posts?category="+encodeURIComponent(currentCategory));$("#count").textContent=d.total;$("#posts").innerHTML=d.posts.length?d.posts.map(p=>'<article class="card" onclick="openPost('+p.id+')"><span class="categoryTag">'+esc(p.category)+'</span>'+(p.image?'<img class="thumb" src="'+esc(p.image)+'" alt="">':"")+'<div class="title">'+esc(p.title)+'</div><div class="meta">'+esc(p.nickname)+' · 조회 '+p.views+' · '+date(p.created_at)+'</div></article>').join(""):'<div class="card">아직 게시글이 없습니다. 첫 글을 남겨보세요.</div>'}async function openPost(id){try{const p=await api("/api/posts/"+id);$("#postDetail").innerHTML='<div class="dialogHead"><div><span class="categoryTag">'+esc(p.category)+'</span><h2>'+esc(p.title)+'</h2></div><button class="x" data-close>×</button></div><div class="meta">'+esc(p.nickname)+' · 조회 '+p.views+'</div><div class="body">'+esc(p.body)+'</div>'+(p.image?'<img class="detailImg" src="'+esc(p.image)+'" alt="첨부 이미지">':"")+'<h3>댓글 '+p.comments.length+'</h3>'+p.comments.map(c=>'<div class="comment"><b>'+esc(c.nickname)+'</b><span class="meta"> · '+date(c.created_at)+'</span><p>'+esc(c.body)+'</p></div>').join("")+'<form class="commentForm" onsubmit="return addComment(event,'+p.id+')"><input name="body" maxlength="2000" placeholder="댓글을 입력하세요" required><button class="primary">등록</button></form>';$("#postDialog").showModal()}catch(e){toast(e.message)}}async function addComment(e,id){e.preventDefault();try{await api("/api/posts/"+id+"/comments",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({body:e.target.body.value})});toast("댓글 등록 완료");await openPost(id)}catch(x){toast(x.message)}return false}async function adminLogin(e){e.preventDefault();try{const d=await api("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({username:$("#adminUser").value,password:$("#adminPass").value})});$("#loginDialog").close();await adminPanel(d.role)}catch(x){toast(x.message)}}async function adminPanel(role){try{const me=await api("/api/admin/me"),posts=await api("/api/admin/posts");role=me.role;let html='<div class="adminHead"><div><p class="eyebrow">PRIVATE CONSOLE</p><h2>운영 패널</h2><div class="meta">'+esc(me.username)+' · '+esc(me.role)+'</div></div><button class="ghost" id="logoutBtn">로그아웃</button></div>'+(role==="admin"?'<section class="supportAdmin"><h3>지원자 / 계정 지급</h3><form id="supportCreateForm" class="staffForm"><input id="supportLabel" placeholder="지원자 이름" maxlength="64" required><button class="primary">지원 코드 발급</button></form><div id="supporterList" class="adminList"></div><form id="accountAssignForm" class="staffForm"><select id="accountSupporter" required><option value="">지원자 선택</option></select><input id="accountUser" placeholder="게임 계정 ID" required><input id="accountPass" type="password" placeholder="게임 계정 비밀번호" required><button class="primary">계정 지급</button></form></section><hr>':'')+'<h3>게시글 관리</h3>';<div class="adminList">'+posts.posts.map(p=>'<div class="adminRow"><div><b>'+esc(p.title)+'</b><div class="meta">'+esc(p.nickname)+' · '+date(p.created_at)+'</div></div><button class="danger" onclick="deletePost('+p.id+')">삭제</button></div>').join("")+'</div>';if(role==="admin"){const st=await api("/api/admin/staff");html+='<hr><h3>운영자 관리</h3><form id="staffForm" class="staffForm"><input id="staffUser" placeholder="아이디" maxlength="64" required><input id="staffPass" type="password" placeholder="10자 이상 비밀번호" minlength="10" required><select id="staffRole"><option value="operator">운영자</option><option value="admin">관리자</option></select><button class="primary">계정 추가</button></form><div class="adminList">'+st.staff.map(s=>'<div class="adminRow"><div><b>'+esc(s.username)+'</b><div class="meta">'+esc(s.role)+'</div></div><select onchange="changeRole('+s.id+',this.value)"><option value="operator" '+(s.role==="operator"?"selected":"")+'>운영자</option><option value="admin" '+(s.role==="admin"?"selected":"")+'>관리자</option></select></div>').join("")+'</div>'}$("#adminPanel").innerHTML=html;$("#adminDialog").showModal();if(role==="admin"){await loadSupporters();$("#supportCreateForm").onsubmit=async e=>{e.preventDefault();try{const d=await api("/api/admin/supporters",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({label:$("#supportLabel").value})});prompt("지원자에게 전달할 지원 코드",d.code);e.target.reset();toast("지원 코드 발급 완료");await loadSupporters()}catch(x){toast(x.message)}};$("#accountAssignForm").onsubmit=async e=>{e.preventDefault();try{await api("/api/admin/game-accounts",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({supporter_id:$("#accountSupporter").value,username:$("#accountUser").value,password:$("#accountPass").value})});e.target.reset();toast("게임 계정 지급 완료");await loadSupporters()}catch(x){toast(x.message)}}}$("#logoutBtn").onclick=async()=>{await api("/api/admin/logout",{method:"POST"});$("#adminDialog").close();toast("로그아웃 완료")};const sf=$("#staffForm");if(sf)sf.onsubmit=async e=>{e.preventDefault();try{await api("/api/admin/staff",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({username:$("#staffUser").value,password:$("#staffPass").value,role:$("#staffRole").value})});toast("운영 계정 추가 완료");adminPanel("admin")}catch(x){toast(x.message)}}}catch(x){toast(x.message)}}async function deletePost(id){if(!confirm("이 게시글을 삭제할까요?"))return;try{await api("/api/admin/posts/"+id,{method:"DELETE"});toast("삭제 완료");adminPanel(me.role)}catch(x){toast(x.message)}}async function changeRole(id,role){try{await api("/api/admin/staff/"+id+"/role",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({role})});toast("권한 변경 완료")}catch(x){toast(x.message)}}$("#supportBtn").onclick=async()=>{try{await api("/api/support/me");await supportPanel()}catch{$("#supportDialog").showModal()}};$("#supportLoginForm").onsubmit=async e=>{e.preventDefault();try{await api("/api/support/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({code:$("#supportCode").value})});$("#supportDialog").close();await supportPanel();toast("지원자 인증 완료")}catch(x){toast(x.message)}};$("#themeBtn").onclick=toggleTheme;$("#writeBtn").onclick=()=>$("#writeDialog").showModal();$("#refreshBtn").onclick=load;$("#nickBtn").onclick=()=>$("#nickDialog").showModal();$("#adminBtn").onclick=async()=>{try{const d=await api("/api/admin/me");adminPanel(d.role)}catch{$("#loginDialog").showModal()}};$("#loginForm").onsubmit=adminLogin;document.addEventListener("click",e=>{if(e.target.matches("[data-close]"))e.target.closest("dialog").close()});$("#nickForm").onsubmit=async e=>{e.preventDefault();try{const d=await api("/api/nickname",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({nickname:$("#nickInput").value})});me.nickname=d.nickname;$("#nickDialog").close();toast("닉네임 저장 완료")}catch(x){toast(x.message)}};$("#postForm").onsubmit=async e=>{e.preventDefault();try{const d=await api("/api/posts",{method:"POST",body:new FormData(e.target)});$("#writeDialog").close();e.target.reset();toast("게시글 등록 완료");await load();openPost(d.id)}catch(x){toast(x.message)}};window.openPost=openPost;window.addComment=addComment;window.deletePost=deletePost;window.changeRole=changeRole;document.querySelectorAll(".category").forEach(b=>b.onclick=async()=>{document.querySelectorAll(".category").forEach(x=>x.classList.remove("active"));b.classList.add("active");currentCategory=b.dataset.category;await load()});theme();(async()=>{try{await session();await load()}catch(e){toast(e.message)}})();
async function supportPanel(){try{const m=await api("/api/support/me"),a=await api("/api/support/account");let account=a.account;$("#supportPanel").innerHTML='<div class="adminHead"><div><p class="eyebrow">SUPPORT ACCESS</p><h2>지원자 전용 센터</h2><div class="meta">'+esc(m.label)+'</div></div><button class="ghost" id="supportLogout">로그아웃</button></div><div class="macroHero"><div><span class="categoryTag">AUTHORIZED</span><h3>캣히어로 웹 매크로</h3><p class="muted">지급된 지원 계정으로 이용하는 전용 도구입니다.</p></div><button class="primary" id="macroOpen">매크로 설정</button></div>'+(account?'<div class="accountCard"><div><b>지급 계정</b><div class="meta">계정 정보는 서버에서 보호된 형태로 저장됩니다.</div></div><button class="ghost" id="showAccount">계정 보기</button></div>':'<div class="card">아직 지급된 게임 계정이 없습니다.</div>');$("#supportPanelDialog").showModal();$("#supportLogout").onclick=async()=>{await api("/api/support/logout",{method:"POST"});$("#supportPanelDialog").close();toast("지원자 로그아웃 완료")};$("#showAccount")?.addEventListener("click",()=>toast("계정 ID: "+account.username+" / 비밀번호: "+account.password));$("#macroOpen").onclick=()=>openMacroSettings()}catch(e){$("#supportDialog").showModal()}}
function openMacroSettings(){ $("#supportPanel").innerHTML='<div class="dialogHead"><div><p class="eyebrow">CAT HERO MACRO</p><h2>웹 매크로 설정</h2></div><button class="x" data-close>×</button></div><div class="macroGrid"><label>반복 간격(ms)<input id="macroInterval" type="number" min="100" max="60000" value="1000"></label><label>반복 횟수<input id="macroRepeat" type="number" min="1" max="10000" value="10"></label></div><div class="macroSteps" id="macroSteps"></div><button class="ghost" id="addMacroStep">＋ 터치 위치 추가</button><div class="actions"><button class="ghost" id="macroReset">초기화</button><button class="primary" id="macroSave">설정 저장</button></div><p class="muted">실제 캣히어로 웹페이지의 클릭 실행은 해당 웹페이지에서 허용된 브라우저 자동화 방식으로 연결해야 합니다.</p>';const steps=$("#macroSteps");let list=JSON.parse(localStorage.getItem("cat-macro-steps")||"[]");const render=()=>{steps.innerHTML=list.map((s,i)=>'<div class="macroStep"><b>'+(i+1)+'</b><input value="'+esc(s.selector||"")+'" placeholder="버튼/요소 선택자"><input type="number" value="'+(s.delay||1000)+'" min="100" max="60000"><button class="danger" onclick="removeMacroStep('+i+')">삭제</button></div>').join("")||'<div class="card">아직 매크로 단계가 없습니다.</div>'};window.removeMacroStep=i=>{list.splice(i,1);render()};$("#addMacroStep").onclick=()=>{list.push({selector:"",delay:1000});render()};$("#macroReset").onclick=()=>{list=[];render()};$("#macroSave").onclick=()=>{localStorage.setItem("cat-macro-steps",JSON.stringify(list));toast("매크로 설정 저장 완료")};render()}
async function loadSupporters(){const d=await api("/api/admin/supporters");$("#supporterList").innerHTML=d.supporters.map(s=>'<div class="adminRow"><div><b>'+esc(s.label)+'</b><div class="meta">'+(s.has_account?"계정 지급됨":"계정 미지급")+' · 매크로 '+(s.macro_enabled?"ON":"OFF")+'</div></div><button class="ghost" onclick="toggleSupporter('+s.id+','+(!s.active)+')">'+(s.active?"비활성화":"활성화")+'</button></div>').join("")||'<div class="card">지원자가 없습니다.</div>';$("#accountSupporter").innerHTML='<option value="">지원자 선택</option>'+d.supporters.filter(s=>s.active).map(s=>'<option value="'+s.id+'">'+esc(s.label)+'</option>').join("");}
window.toggleSupporter=async(id,active)=>{try{await api("/api/admin/supporters/"+id,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({active})});toast("지원자 상태 변경 완료");await loadSupporters()}catch(e){toast(e.message)}};
